#!/usr/bin/env bash
# Stub: PyInstaller binary build. Implemented in Phase 6 (Packaging).
#
# Placeholder so CI can reference the expected path without surprises.

set -euo pipefail
echo "rc0 binary build is implemented in Phase 6 — see mission plan §16." >&2
exit 64  # EX_USAGE
